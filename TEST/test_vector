#include<iostream>
#include<vector>
#include<algorithm>
using namespace std;
int main()
{
//	int b[7]={1,2,3,4,5,9,8};
    //    vector<int> d(b,b+7);
     //   sort(d.begin() ,d.end()); /// /从小到大
		 
   /* int a[6]={1,2,3,4,5,6};
vector<int> b(a,a+4);
for(vector<int>::iterator it=b.begin();it!=b.end();it++){
cout<<*it<<" ";
 b[*it] = 0;//将向量b元素全部置0
}*/

   /* vector<int> f;   //这段是错的，下标只能用于获取已存在的元素，而现在的f[i]还是空的对象 
for(int i=0;i<10;i++){
    f[i]=i;  
        cout<<f[i]<<endl;
    }
      */  
      
        
        int a[6]={1,2,3,4,5,6};
vector<int> b(a,a+4);
for(int i=0;i<=b.size()-1;i++)
    cout<<b[i]<<" ";
 
 
    return 0;
}
